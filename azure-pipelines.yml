trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: "3.12"

  - script: |
      pip install -r requirements.txt
      pytest
    displayName: Run unit tests

  - script: docker build -t testrms:$(Build.SourceVersion) .
    displayName: Build container
