apiVersion: v1
kind: Service
metadata:
  name: testrms-canary
  labels:
    app.kubernetes.io/name: testrms
    app.kubernetes.io/role: canary
spec:
  selector:
    app.kubernetes.io/name: testrms
  ports:
    - name: http
      port: 80
      targetPort: http
